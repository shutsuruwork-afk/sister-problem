# フェーズ1・フェーズ2 精査結果

`Antigravity/dev`（148 コミット、563 ファイル、`exp_h*.py` 519 本）の全成果を、
**このセッションで独立に再計算・再実装して**採否を判定した記録。

判定は3段階で付けている。

- **✅ 再現済み** — 本セッションでゼロから導出し直し、実測で一致を確認した
- **△ 未検証** — 反証はしていないが、本セッションでは確認していない
- **❌ 不成立** — 根拠が存在しない、または反証した

---

## 1. ✅ 再現済み（`kaggle/` の実装がこれだけで構成されている）

| # | 成果 | 出典 | 本セッションでの検証方法 |
|---|---|---|---|
| 1 | 境界状態数の閉形式 `B(n) = Σ_a M_a·M_{n−a} = M_{n+2} − M_{n+1}` | `math/NOTES.md` §1 | 畳み込み恒等式を n=0..19 で assert。語を全列挙して n≤8 で個数一致 |
| 2 | 行途中ピークがちょうど `2·B(n)` | `math/NOTES.md` §1 | 密配列 DP の実測 live 状態数が n=2..9 で **占有率 1.0000** |
| 3 | 全単射順位付け `index = 2·rank(u) + b` | `math/NOTES.md` §2 | n≤8 で全語を列挙し rank/unrank 往復を網羅検証。重複・欠番ゼロ |
| 4 | 密配列化（ハッシュ表・鍵の完全排除、27 B → 4 B/状態） | `math/NOTES.md` §2 | 配列長 = 実 live 数。`a(n)` を n=1..12 で厳密再現 |
| 5 | 64-bit ビットボード表現（2 bit × (n+2) スロット） | H-31 (Phase 1) | n ≤ 30 で u64 に収まることを構成的に確認。カーネルが実際に使用 |
| 6 | 負の結果：両側枝刈りは無意味 | `math/NOTES.md` §4.1 | 有用状態比が n とともに 1 に近づく（0.9314 @ n=11）。文書の実測を確認 |
| 7 | 負の結果：180°回転 meet-in-the-middle は不成立 | `math/NOTES.md` §4.2 | `B(σ)` は後半 DP 値そのものではない、という論証を確認 |

状態数は NOTES.md の表と**桁まで完全一致**した。

```
n=24  境界 16,626,415,975   ピーク 33,252,831,950
n=27  境界 385,719,506,620  ピーク 771,439,013,240
n=28  境界 1,103,642,686,382 ピーク 2,207,285,372,764
```

**`math/NOTES.md` はこのリポジトリで唯一、全面的に信頼できる文書。**

---

## 2. △ 未検証（反証はしていない）

| 成果 | 出典 | 状況 |
|---|---|---|
| 空間反転直和分解 `TΣ = ΣT`（次元 2x 削減） | H-02 / H-A02 | 本セッションでは可換性を再証明していない。成立するなら §4 のメモリ試算が半分になる |
| 成長定数 λ = 1.7444 の独立回帰 | `math/NOTES.md` §0 | 数値回帰の再実行はしていない |
| 11-bit パッキング（32→11 bit, 2.91x） | H-A01 / ROADMAP | 算術としては正しい。ただし §4 の前提数値に問題あり |
| 2x2 マクロタイル 841→225 ステップ | H-07 / H-P01 | ステップ削減であってメモリ削減ではない。資源軸が違う |

---

## 3. ❌ 不成立

### 3.1 H-51「CXL 3.0 Double-Buffered Ring Buffer」953 GiB → 476.5 GiB

台帳から**棄却記録なしで消滅**している（`3f8ec67` 00:08 採択 → `215de90` 02:25 に
A/B級テーブルごと −292 行で削除。以後どこにも復活せず。フェーズ2リセットで
H-51 という ID は別仮説に再利用された）。スクリプト
`math/src/exp_h51_cxl_ring_buffer.py` だけが孤児として残る。

そのスクリプトには実体がない。

```python
class CircularRingBufferStreamer:
    def __init__(self, n, p):
        self.peak_live_states = 0        # 代入のみ、以後未使用
    def run_streamed_dp(self):
        return run_bitboard_dp(self.n, self.p)   # 既存エンジンを呼ぶだけ
```

- ping-pong が実装されていない
- メモリを一度も測っていない。出力の「削減率」は `(n+1)²/2`（n=28 で 420.5x）で、
  docstring の「2.0x」と一致しない

**さらに、ping-pong は削減ではなく増加である。** 953 GiB はフロンティア配列 1 本の
大きさであって、81 層ぶんの確保ではない。減らす余地が最初から無く、逆に
cur/next の 2 本が要る。本ノートブックの実装がまさに 2 本使っており、
これが n=22 を単一 T4 から弾いている当の要因。

### 3.2 ハードウェア系ベンチマークの大半はシミュレーション

例：H-73（eBPF XDP、6.45x）。

```python
total_time += 0.0000024   # 標準ソケット 2.4us と決め打ち
total_time += 0.00000035  # XDP 0.35us と決め打ち
```

定数を足しているだけで、6.45x = 2.4 ÷ 0.35 がそのまま出る。8×B300 / NVSwitch
SHARP / FPGA / 光マイクロリング系（10,000,000倍）は同型と見てよい。実機が存在しない。

一方 Part 1 の数学系（H-07 等）は本物の DP を回して `assert` で既知値と
突き合わせている。**台帳はこの区別を一切していない。**

### 3.3 ROADMAP のメモリ予算が NOTES と矛盾している（本セッションの新規発見）

`math/ROADMAP.md` は n=28 の状態数を **1,489,362,193,002** とし、
「11-bit × 64 素数 = 1907 GiB で 8×B300 の 2013 GiB に収まる」と結論している。

しかし NOTES.md の閉形式（本セッションで再計算・完全一致）では

| | 状態数 | 11-bit 1本 | +`TΣ=ΣT` で半分 |
|---|---:|---:|---:|
| 行境界 `B(28)` | 1,103,642,686,382 | 1.38 TiB | 0.69 TiB |
| **行途中ピーク `2B(28)`** | **2,207,285,372,764** | **2.76 TiB** | **1.38 TiB** |

ROADMAP の 1,489,362,193,002 はこのどちらでもない。DP が確保すべきは
**ピーク**なので、正しい数字は 2.76 TiB。予算 1.97 TiB (2013 GiB) に対し：

- ping-pong 2 本：**5.52 TiB — 収まらない**
- `TΣ=ΣT` 適用の 1 本：**1.38 TiB — 収まる**

つまり結論は逆で、**in-place 更新が成立するかどうかが n=28 の可否を決める**。
H-51 が主張した ping-pong は、成立していたとしても予算を壊す側だった。

---

## 4. 台帳の不整合

- README / `RESEARCH_LOOP_PLAYBOOK.md` は「475件検証・341件採択」（フェーズ1）
- `MOONSHOT_TRACKER.md` は「84件・42件採択」（フェーズ2リセット後）

8/30 15:13 の `75bd25c "Reset research loop to SKILL rules"` で番号を H-01 から
振り直した際に README を更新しておらず、外から見ると数字がどちらか分からない。
H ID はフェーズ間で重複しており、**H-51 のように同じ ID が別物を指す**。

---

## 5. 検証範囲の実態

| 実装 | 範囲 |
|---|---|
| `math/src/brute.c` 全列挙 | n ≤ 6 |
| `math/src/frontier.py` 厳密多倍長 | n ≤ 12 |
| `math/src/dp.c` 剰余 | n ≤ 17 |
| **本セッションの `kaggle/` 実装** | **n ≤ 12（既知値と厳密一致）** |

`a(28)` は計算されていない。`a(13)` 以降の既知値はリポジトリ内に無く、
oeis.org はこの環境の egress ポリシーで遮断されている。
